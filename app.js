/* =============================================
   Hostinger / cPanel-style hosts expect an `app.js` entry by default.
   This file just delegates to server.js so we don't duplicate logic.
   ============================================= */
require('./server.js');
